name: 'Github-Auto-Follow-Unfollow-View-User-List'
description: '💡 Auto follow, unfollow, update user list(unfollow, unfollowing, follower, following) by Cron JOB ( every 8 hours )'
author: 'Huniko519'

inputs:
  token:
    description: 'The token used to authenticate.'
    required: true
    default: ${{ github.token }}

  username:
    description: 'The base user name.'
    required: true
    default: ${{ github.actor }}

  repository:
    description: 'The name of the repository.'
    required: true
    default: ${{ github.repository }}
    
runs:
  using: 'node16'
  main: 'main.js'

branding:
  icon: 'check-square'
  color: 'green'
